# 📋 AI Skill Monitor — 完整项目交接文档

> 看完这份文档，你可以独立理解、运行和修改本项目的全部内容。

---

## 一、项目简介

**AI Skill Monitor** 是一个自动监控 AI 技能与开源工具的看板网站，分两个版块：

| 版块 | 内容 |
|------|------|
| **HR 技能监控** | 监控 Skillhub、GitHub 上 HR（人力资源）相关的 AI 技能和开源项目 |
| **办公通用技能** | 监控 Skillhub、GitHub 上办公通用类 AI 技能和工具 |

**运行方式：** GitHub Actions 每天北京时间 09:00 自动运行 Python 脚本抓取数据，生成 Markdown 文件，Cloudflare Pages 自动从 GitHub 拉取并部署静态网站。

**线上地址：** https://ffff.avazhang1224.workers.dev/  
**GitHub 仓库：** https://github.com/Avaaaaa124/ffff.git

---

## 二、目录结构

```
hr-agent-monitor/
├── index.html                    # ★ 网站唯一前端文件（单页应用）
├── requirements.txt              # Python 依赖（只有 requests）
├── DEPLOY.md                     # 部署步骤说明
├── README.md                     # 项目说明
│
├── scripts/
│   ├── fetch_hr_projects.py      # ★ HR 版块数据抓取脚本
│   └── fetch_office_skills.py    # ★ 办公版块数据抓取脚本
│
├── data/                         # 由脚本自动生成，不需要手动修改
│   ├── skills_today.md           # HR 技能今日数据（含嵌入 JSON）
│   ├── github_today.md           # HR GitHub 项目今日数据（含嵌入 JSON）
│   ├── skills_history.md         # HR 技能历史记录
│   ├── github_history.md         # HR GitHub 项目历史记录
│   ├── history.json              # HR 数据历史 JSON
│   ├── office_skills_today.md    # 办公技能今日数据（含嵌入 JSON）
│   ├── office_github_today.md    # 办公 GitHub 项目今日数据（含嵌入 JSON）
│   ├── office_history.md         # 办公技能历史记录
│   └── office_history.json       # 办公数据历史 JSON
│
└── .github/
    └── workflows/
        └── daily_update.yml      # ★ GitHub Actions 定时任务配置
```

---

## 三、技术栈

| 层 | 技术 |
|----|------|
| 前端 | 单文件 `index.html`（原生 JS + CSS，引用 CDN 上的 `marked.js`） |
| 数据获取 | Python 3.11 + `requests` 库 |
| 数据存储 | Markdown 文件（内嵌 JSON 注释块，供前端解析） |
| CI/CD | GitHub Actions（定时 + 手动触发） |
| 部署 | Cloudflare Pages（静态托管，自动同步 GitHub main 分支） |
| 翻译 | MyMemory 免费翻译 API（`api.mymemory.translated.net`，无需 key） |

---

## 四、数据流（完整链路）

```
每天 09:00 (北京时间)
        ↓
GitHub Actions 触发
        ↓
运行 fetch_hr_projects.py
  → 搜索 GitHub API（star>200 的 HR 相关仓库）
  → 搜索 Skillhub API（https://api.skillhub.cn/api/skills）
  → 搜索 Clawhub API（https://clawhub.cn/api/mcp/search）
  → HR 相关性二次过滤（白名单/黑名单/上下文匹配）
  → 翻译英文描述为中文（MyMemory API）
  → 生成 data/github_today.md（Markdown + 内嵌 GITHUB_DATA JSON）
  → 生成 data/skills_today.md（Markdown + 内嵌 SKILL_DATA JSON）
  → 更新 data/history.json / github_history.md / skills_history.md
        ↓
运行 fetch_office_skills.py（同上流程，针对办公通用类）
  → 生成 data/office_skills_today.md（内嵌 OFFICE_SKILL_DATA JSON）
  → 生成 data/office_github_today.md（内嵌 OFFICE_GITHUB_DATA JSON）
  → 更新 data/office_history.json / office_history.md
        ↓
git commit & push → GitHub 仓库 main 分支
        ↓
Cloudflare Pages 检测到 push → 自动重新部署
        ↓
用户访问网站，浏览器 fetch 加载 data/*.md 文件
        ↓
前端 JS 解析 Markdown 内嵌的 JSON，渲染卡片
```

---

## 五、前端（index.html）详解

### 5.1 整体结构

```html
index.html
├── <head>         CSS 样式（全部内联，约 700 行）
├── <body>
│   ├── .header    顶部标题栏
│   ├── .section-nav  版块切换按钮（HR / 办公）
│   ├── #hr-section   HR 技能版块（初始 active）
│   └── #office-section  办公版块（懒加载）
└── <script>       所有 JS 逻辑（约 700 行）
```

### 5.2 HR 版块（#hr-section）

```
#hr-section
├── .stats-row         顶部统计卡片
│   ├── #stat-skillhub    Skillhub Skills 数量
│   ├── #stat-github      GitHub 项目数量
│   └── #stat-date        最近更新日期
│
└── .tab-container     Tab 切换区
    ├── Tab①「今日技能」→ #panel-today-skills
    │     渲染 skillhub 技能卡片（#skills-grid）
    │     支持：搜索、按热度/下载/Stars/名称排序、分类过滤
    │
    ├── Tab②「今日 GitHub」→ #panel-today-github
    │     渲染 GitHub 项目卡片（#github-grid）
    │     支持：搜索、排序、分类过滤
    │
    ├── Tab③「历史技能」→ #panel-history-skills
    │     渲染 skills_history.md（Markdown）
    │
    └── Tab④「历史 GitHub」→ #panel-history-github
          渲染 github_history.md（Markdown）
```

### 5.3 办公版块（#office-section）

结构与 HR 版块镜像，懒加载（首次切换到办公 Tab 时才加载数据）：

```
#office-section
├── .stats-row
│   ├── #stat-office-skills   办公技能数量
│   ├── #stat-office-github   GitHub 办公工具数量
│   └── #stat-office-date     最近更新日期
│
└── .tab-container
    ├── Tab①「办公技能总览」→ #panel-office-today-skills
    │     渲染 office_skills_today.md 中的 JSON 数据
    │     支持：搜索、排序、10 大分类过滤
    │
    ├── Tab②「GitHub 办公工具」→ #panel-office-today-github
    │     渲染 office_github_today.md 中的 JSON 数据
    │
    └── Tab③「历史记录」→ #panel-office-history
          渲染 office_history.md（Markdown）
```

### 5.4 关键 JS 函数速查

| 函数名 | 作用 |
|--------|------|
| `loadAllData()` | 页面启动时加载 HR 版块的 4 个 MD 文件 |
| `extractStats(githubMd, skillsMd)` | 从 MD 文本解析统计数字，填充 stat-card |
| `extractSkillData(mdText)` | 解析 `<!-- SKILL_DATA:...:SKILL_DATA -->` 中的 JSON |
| `extractGithubData(mdText)` | 解析 `<!-- GITHUB_DATA:...:GITHUB_DATA -->` 中的 JSON |
| `renderSkillCards(data)` | 渲染 HR 技能卡片到 `#skills-grid` |
| `renderGithubCards(data)` | 渲染 HR GitHub 项目卡片到 `#github-grid` |
| `filterSkills()` | HR 技能搜索 + 分类过滤（用全局 `allSkillData`） |
| `sortSkills(btn, field)` | HR 技能排序 |
| `switchSection(id)` | 切换 HR/办公版块，触发办公版块懒加载 |
| `loadOfficeData()` | 懒加载办公版块的 3 个 MD 文件 |
| `extractOfficeSkillData(mdText)` | 解析 `<!-- OFFICE_SKILL_DATA:...:OFFICE_SKILL_DATA -->` |
| `extractOfficeGithubData(mdText)` | 解析 `<!-- OFFICE_GITHUB_DATA:...:OFFICE_GITHUB_DATA -->` |
| `renderOfficeSkillCards(data)` | 渲染办公技能卡片 |
| `renderOfficeGithubCards(data)` | 渲染办公 GitHub 卡片 |
| `filterOfficeSkills()` | 办公技能搜索 + 分类过滤 |
| `switchOfficeTab(id)` | 切换办公版块内部 Tab |

### 5.5 数据解析格式（嵌入 JSON）

前端通过正则从 MD 文件中提取 JSON：

```javascript
// HR 技能
mdText.match(/<!-- SKILL_DATA:(.+?):SKILL_DATA -->/s)
// 结构：{ skillhub: [...], clawhub: [...] }

// HR GitHub 项目
mdText.match(/<!-- GITHUB_DATA:(.+?):GITHUB_DATA -->/s)
// 结构：[{ name, url, desc, desc_zh, stars, forks, language, topics, hr_category }]

// 办公技能
mdText.match(/<!-- OFFICE_SKILL_DATA:(.+?):OFFICE_SKILL_DATA -->/s)
// 结构：{ office_skills: [...] }

// 办公 GitHub
mdText.match(/<!-- OFFICE_GITHUB_DATA:(.+?):OFFICE_GITHUB_DATA -->/s)
// 结构：[{ name, url, desc, desc_zh, stars, forks, language, office_category }]
```

### 5.6 stat-card 统计数字解析

从 MD 文件头部文本提取（正则匹配，**注意这些元素 ID 必须存在**）：

```javascript
// HR GitHub 项目数 → #stat-github
githubMd.match(/项目总数\s*\|\s*(\d+)/)

// HR Skillhub Skills 数 → #stat-skillhub
skillsMd.match(/Skillhub[：:]\s*\*\*(\d+)\*\*/)

// Skillhub 总下载量 → #stat-skillhub-sub
skillsMd.match(/总下载量\s*\|\s*([\d,]+)/)

// 办公技能数 → #stat-office-skills
text.match(/技能总数\s*\|\s*\*\*(\d+)\*\*/)

// 办公 GitHub 项目数 → #stat-office-github
text.match(/项目总数\s*\|\s*\*\*(\d+)\*\*/)
```

---

## 六、Python 脚本详解

### 6.1 fetch_hr_projects.py（HR 版块）

**关键配置（脚本顶部）：**

```python
STAR_THRESHOLD = 200           # GitHub 项目最低 Star 数
SKILL_PAGE_SIZE = 50           # Skillhub 每页获取数量
SKILL_MAX_PER_KW = 100         # 每个关键词最多取多少条
```

**HR 8 大分类（`HR_CATEGORIES`）：**
1. 招聘与人才获取
2. 薪酬福利与绩效管理
3. 员工关系与劳动合规
4. 培训与人才发展
5. 组织发展与组织设计
6. HR 数字化与系统工具
7. 人才测评与胜任力模型
8. 企业文化与员工体验

**过滤逻辑（`_is_hr_related`）：**
- 黑名单优先：名称/描述含股票、交易、电商、游戏等词直接排除
- 强 HR 词命中即通过
- 模糊词（assessment/analytics 等）需与 HR 上下文词同现才通过

**API 接口：**
- Skillhub: `GET https://api.skillhub.cn/api/skills?keyword=xxx&pageSize=50&pageNumber=1`
  - 响应：`{ code: 0, data: { skills: [...] } }`
  - 字段：`slug, name, description, description_zh, stars, downloads, installs, tags, source, ownerName, homepage`
- GitHub: `GET https://api.github.com/search/repositories?q=xxx&sort=stars&per_page=30`
- Clawhub: `GET https://clawhub.cn/api/mcp/search?q=xxx&limit=30`

**热度公式：**
```python
heat = downloads + installs * 2 + stars * 3
```

### 6.2 fetch_office_skills.py（办公版块）

结构与 HR 脚本相同，差异如下：

| 项目 | HR 脚本 | 办公脚本 |
|------|---------|---------|
| Star 阈值 | 200 | 50（更低，办公工具普遍 Star 少） |
| 分类数 | 8 大 HR 分类 | 10 大办公分类 |
| 分类变量 | `HR_CATEGORIES` | `OFFICE_CATEGORIES` |
| 过滤函数 | `_is_hr_related()` | `_is_office_related()` |
| 分类函数 | `_classify_github_category()` | `_classify_office_skill()` |
| JSON 标记 | `SKILL_DATA` / `GITHUB_DATA` | `OFFICE_SKILL_DATA` / `OFFICE_GITHUB_DATA` |
| 统计字段 | `技能总数 \| **N**` | 同上 |

**办公 10 大分类（`OFFICE_CATEGORIES`）：**
1. 办公软件与文档处理
2. 数据分析与可视化
3. 演示与信息设计
4. 项目管理与协作
5. 文案撰写与内容生成
6. 思维导图与知识管理
7. 流程优化与自动化
8. 协同办公与沟通
9. 表格处理与报表
10. AI 办公工具

---

## 七、GitHub Actions（daily_update.yml）

**触发方式：**
- 定时：每天 UTC 01:00（北京时间 09:00）
- 手动：仓库 → Actions → 「Run workflow」

**执行步骤：**
1. checkout 代码
2. 安装 Python 3.11 + `pip install requests`
3. 运行 `scripts/fetch_hr_projects.py`（需要 `GITHUB_TOKEN`、可选 `WECHAT_WEBHOOK`）
4. 运行 `scripts/fetch_office_skills.py`
5. `git add data/ && git commit && git push`（只有数据有变化才 push）
6. 输出摘要到 Actions 日志

**所需 GitHub Secrets：**

| Secret 名 | 说明 | 是否必须 |
|-----------|------|---------|
| `GITHUB_TOKEN` | 自动注入，无需手动配置 | 必须 |
| `WECHAT_WEBHOOK` | 企业微信机器人 Webhook URL | 可选（不配置则跳过通知） |

**常见问题：**
- Actions push 失败（403）：仓库 Settings → Actions → General → Workflow permissions → 选「Read and write permissions」

---

## 八、Cloudflare Pages 部署

**配置要点：**

| 项 | 值 |
|----|----|
| 连接仓库 | GitHub → `ffff`（或你的仓库名） |
| Production branch | `main` |
| Framework preset | `None` |
| Build command | **留空** |
| Build output directory | `/` |

**工作原理：** GitHub main 分支有 push → Cloudflare 自动拉取 → 静态文件直接对外提供（无构建步骤）。

---

## 九、如何本地运行

### 9.1 环境准备

```bash
pip install requests
```

### 9.2 运行 HR 脚本

```bash
cd hr-agent-monitor/scripts
python fetch_hr_projects.py
# 输出：data/github_today.md, data/skills_today.md 等
```

可选：设置 GitHub Token 避免 API 限速（未认证 60次/小时，认证 5000次/小时）：

```bash
# Windows PowerShell
$env:GITHUB_TOKEN = "ghp_xxxx"
python fetch_hr_projects.py
```

### 9.3 运行办公脚本

```bash
cd hr-agent-monitor/scripts
python fetch_office_skills.py
```

### 9.4 本地预览网站

直接在浏览器打开 `index.html` **不行**（fetch 同源限制），需要起一个 HTTP 服务器：

```bash
cd hr-agent-monitor
python -m http.server 8080
# 然后访问 http://localhost:8080
```

---

## 十、如何修改和扩展

### 10.1 修改监控范围

**增减 HR 搜索关键词：** 编辑 `scripts/fetch_hr_projects.py` 中的 `GITHUB_SEARCH_QUERIES` 列表和 `HR_CATEGORIES` 字典。

**修改 Star 过滤阈值：** 修改脚本顶部的 `STAR_THRESHOLD = 200`。

**调整办公技能分类：** 编辑 `scripts/fetch_office_skills.py` 中的 `OFFICE_CATEGORIES` 字典。

**注意：** 修改分类后，前端 `index.html` 中的分类过滤下拉框也需要同步修改（搜索 `office-category-filter` 或 `category-filter`）。

### 10.2 修改前端样式

所有 CSS 在 `index.html` 的 `<style>` 标签中，关键 CSS 变量：

```css
:root {
  --bg: #0f1117;        /* 页面背景 */
  --card: #1a1f2e;      /* 卡片背景 */
  --accent2: #22d3ee;   /* 主强调色（青色） */
  --green: #4ade80;     /* 绿色 */
  --purple: #a78bfa;    /* 紫色 */
  --border: rgba(255,255,255,0.08);
}
```

### 10.3 添加新版块

1. 在 `index.html` 中的 `.section-nav` 添加新按钮
2. 添加新的 `<div class="section-panel" id="xxx-section">` 结构
3. 仿照 `loadOfficeData()` 写新的数据加载函数
4. 在 `switchSection(id)` 中添加懒加载触发逻辑
5. 写对应的 Python 数据抓取脚本
6. 在 `.github/workflows/daily_update.yml` 中添加执行步骤

### 10.4 修改自动更新时间

编辑 `.github/workflows/daily_update.yml`：

```yaml
schedule:
  - cron: '0 1 * * *'   # UTC 01:00 = 北京时间 09:00
  # 改成 UTC 02:00 = 北京时间 10:00：
  # - cron: '0 2 * * *'
```

### 10.5 新部署到其他账号

1. 在新 GitHub 账号创建仓库，将本项目代码 push 上去
2. 仓库 Settings → Actions → General → Workflow permissions → 选「Read and write permissions」
3. 在 Cloudflare Pages 连接新仓库，按「八、Cloudflare Pages 部署」章节配置
4. 可选：添加 `WECHAT_WEBHOOK` Secret 接收微信通知
5. 手动触发一次 Actions 生成初始数据

---

## 十一、已知问题与坑点

| 问题 | 原因 | 解决方案 |
|------|------|---------|
| stat-card 数字显示"—" | `extractStats` 中调用了不存在的 `tag-github`/`tag-skillhub` 元素，报 null 错误后被 `catch` 吞掉 | 已修复：改为先判断元素是否存在 |
| 办公版块内容空白 | JS 解析数据的正则未匹配到 JSON 块 | 查看 Console 日志，确认 MD 文件是否正确生成 |
| GitHub push 失败（公司网络） | 公司网络封锁 HTTPS TCP 数据传输 | 切换到手机热点或家庭网络 push |
| GitHub API 限速 | 未认证每小时 60 次 | 确保 `GITHUB_TOKEN` Secret 已配置 |
| Cloudflare 缓存旧版本 | CDN 边缘缓存 | 强制刷新（Ctrl+F5）或等缓存过期 |
| 翻译 API 失败 | MyMemory 每日免费额度有限 | 失败时回退到英文原文，不影响功能 |

---

## 十二、文件修改速查表

| 你想改什么 | 改哪个文件 | 改哪里 |
|-----------|-----------|-------|
| 网站样式/布局/颜色 | `index.html` | `<style>` 标签内 |
| HR 版块功能/交互 | `index.html` | `<script>` 标签内 `loadAllData`、`renderSkillCards` 等函数 |
| 办公版块功能/交互 | `index.html` | `<script>` 标签内 `loadOfficeData`、`renderOfficeSkillCards` 等函数 |
| HR 监控的关键词/分类 | `scripts/fetch_hr_projects.py` | `GITHUB_SEARCH_QUERIES`、`HR_CATEGORIES` |
| HR 相关性过滤规则 | `scripts/fetch_hr_projects.py` | `HR_WHITELIST_WORDS`、`HR_BLACKLIST_WORDS` |
| 办公技能的分类 | `scripts/fetch_office_skills.py` | `OFFICE_CATEGORIES` |
| 自动更新时间 | `.github/workflows/daily_update.yml` | `cron:` 字段 |
| 微信通知内容 | `scripts/fetch_hr_projects.py` | `send_wechat()` 调用处（文件末尾） |
| 网站顶部标题 | `index.html` | 搜索 `AI SKILL MONITOR` |

---

*文档生成时间：2026-06-02 | 项目地址：https://github.com/Avaaaaa124/ffff.git*
